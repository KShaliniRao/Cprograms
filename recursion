//PROGRAM TO PRINT THE SUM OF THE DIGITS USING FUNCTION RECURSION

#include<stdio.h>

int fun(int n)
{
    if(n==0)
        return 0;
        return(n%10+fun(n/10));
}

void main()
{
  int num;
  printf("\n Enter the number:");
  scanf("%d",&num);

  printf("\nSum of the digits is:%d",fun(num));
}


OUTPUT=>

 Enter the number:3256

Sum of the digits is:16
